setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    POSTGRES_PASSWORD: docker
build:
  docker:
    web: playmaker/Dockerfile
    worker: postgres/Dockerfile
#  config:
#    BUILD_VAR:value
#release:
#  command:
#    - ./deployment-tasks.sh
#  image: worker
#run:
#  web: bundle exec puma -C config/puma.rb
#  worker: python myworker.py
#  asset-syncer:
#    command:
#      - python asset-syncer.py
#    image: worker